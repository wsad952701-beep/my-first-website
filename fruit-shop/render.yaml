services:
  - type: web
    name: fruit-porter-shop
    runtime: node
    rootDir: fruit-shop/backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
